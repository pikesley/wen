<ul class='nav nav-tabs' id='main-nav'>
  <% Wen::Config.instance.config['pages'].each do |page| %>
    <li role='presentation' <%= "class='active'" if request.path == "/#{page}" %>>
      <a href="/<%= page %>"><%= page %></a>
    </li>
  <% end %>
</ul>
